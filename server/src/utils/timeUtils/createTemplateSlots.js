export const createTemplateSlots = () => {
    // build template slots (DateTimes) for that day in doctor's timezone
    const templateSlots = [];
    for (const e of entries) {
        for (const hhmm of (e.slots || [])) {
            
        }
    }
    return templateSlots;
}